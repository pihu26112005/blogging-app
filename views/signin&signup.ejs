<!DOCTYPE html>
<html lang="en">
<head>
   <%- include('./partials/head') %>
   <title>Home Page</title>
   <style>
       .message {
           font-size: 24px;
           text-align: center;
           margin-top: 50px;
       }
       .buttons {
           display: flex;
           justify-content: center;
           gap: 20px;
           margin-top: 30px;
       }
       .buttons button {
           padding: 10px 20px;
           font-size: 20px;
           cursor: pointer;
       }
   </style>
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="message">
        Please sign in/sign up first to see public blogs
    </div>
    <div class="buttons">
        <button onclick="location.href='/user/signin'">Sign In</button>
        <button onclick="location.href='/user/signup'">Sign Up</button>
    </div>

    <%- include('./partials/script') %>
</body>
</html>